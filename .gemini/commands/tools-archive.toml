prompt = """
If {{step}}:

- Extract section {{step}} in docs/project-plan.md to `docs/dev-logs/{date}-{time}-{hash}-{name of step}.md`
- Replace removed section with

  ```markdown
  ### ✅ {name of step}

  Complete [See](./docs/dev-logs/{date}-{time}-{hash}-{name of step}.md)
  ```

  else:

- Archive the whole `docs/project-plan.md` to `docs/dev-logs/{date}-{time}-{hash}-{description}.md` (e.g. `2024-11-12-1430-87356bd-demo-customers-removal.md`).
- reset `docs/project-plan.md` to clean template:

  ```markdown
  # Project Plan

  Ready for the next plan
  ```

Create dir if needed. Show filename created.

Use `date +%Y-%m-%d-%H%M` for the date-time portion (YYYY-MM-DD-HHMM format).
"""
